# alpakka.google.cloud.pubsub.grpc {
#  host = "localhost"
#  host = ${?V_GOOGLE_PUBSUB_HOST}
#  port = 8538
#  port = ${?V_GOOGLE_PUBSUB_PORT}
#  use-tls = false
#  use-tls = ${?V_GOOGLE_PUBSUB_USE_TLS}
#  rootCa = "none"
#  rootCa = ${?V_GOOGLE_PUBSUB_ROOT_CA}
#  callCredentials = "none" # no authentication
#  callCredentials = ${?V_GOOGLE_PUBSUB_CALL_CREDENTIALS}
#}
